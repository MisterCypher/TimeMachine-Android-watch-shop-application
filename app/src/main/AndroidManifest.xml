<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.timemachine">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.TimeMachine">
        <activity android:name=".Watch10DetailsActivity"></activity>
        <activity android:name=".Watch9DetailsActivity" />
        <activity android:name=".Watch8DetailsActivity" />
        <activity android:name=".Watch7DetailsActivity" />
        <activity android:name=".Watch6DetailsActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".Watch5DetailsActivity" />
        <activity android:name=".Watch4DetailsActivity" />
        <activity android:name=".Watch3DetailsActivity" />
        <activity android:name=".Watch2DetailsActivity" />
        <activity android:name=".Watch1DetailsActivity" />

        <activity android:name=".Watch1Activity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".LoadingScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>